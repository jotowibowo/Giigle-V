# Materi Pelajaran Bagian-2 :
|  No  |              Materi              |
|------|----------------------------------|
|     1| Kombinasi Grafik                 |
|     2| Mengelola tata letak multi grafik|

# 1. Kombinasi Grafik
- Kombinasi scatter dan plot :
  > fig, ax1 = plt.subplots(figsize=(8, 8)) </br>
  > ax2 = ax1.twinx() </br>
  > ax1.scatter(x, y) </br>
  > ax2.plot(x, y, color='green', lw=4) </br>
  > ax1.set_xlabel("xlabel") </br>
  > ax1.set_ylabel("ylabel", color='black', fontsize=14) </br>
  > ax1.tick_params(axis="y", labelcolor='black') </br>
  > ax2.set_ylabel("ylabel", color='red', fontsize=14) </br>
  > ax2.tick_params(axis="y", labelcolor='grey') </br>
  > fig.autofmt_xdate() </br>
  > fig.suptitle("title", fontsize=20 </br>)
  > plt.show()

- Kombinasi boxplot dan plot :
  > plt.figure(figsize=(8, 6)) </br>)
  > sns.boxplot(x='x', y='y', data=df) </br>)
  > plt.plot(x, y, color='red', marker='o', linestyle='--', label='label') </br>)
  > plt.title('title') </br>)
  > plt.xlabel('xlabel') </br>)
  > plt.ylabel('ylabel') </br>)
  > plt.legend() </br>)
  > plt.grid(axis='y', linestyle='--', alpha=0.7) </br>)
  > plt.show()



# 2. Mengelola tata letak multi grafik
- Tata letak 2,1 :
  > w = 0.04
  > fig, axs = plt.subplots(1, 2, figsize=(10,3))
  > bar1 = axs[0].bar(x, y,w, label = 'label1') 
  > bar2 = axs[0].bar(x+w, y,w, label = 'label2')
  > axs[0].set_xlabel("xlabel") 
  > axs[0].set_ylabel('ylabel') 
  > axs[0].set_title("title")
  > axs[0].legend((bar1, bar2),("title1", "title2"))
  > df_box = df_box.rename({var:'Nilai','Id':'Nomor'},axis='columns')
  > df_box = df_box[(df_box['Nilai']<lma) & (df_box['Nilai']>=lmb)]
  > data = df_box[['Nilai','yr']]
  > group_means=data.groupby(['yr'])['Nilai'].mean().sort_values(ascending=True)
  > sns.pointplot(x="yr", y="Nilai", data=data, color = 'black', estimator=np.mean, ax=axs[1])
  > sns.boxplot(x="yr", y="Nilai", data=data,order=group_means.index, ax=axs[1])
  > axs[1].set_title(var.upper())
  > plt.show()
